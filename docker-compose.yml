# 一键运行（进阶需求4 替代方案）：docker compose up --build
# 后端 8000 + 前端静态由后端挂载(dist)，单容器即可访问 http://localhost:8000
services:
  diet-advisor:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env   # 不存在则用 .env.example 默认值（降级模式）
    restart: unless-stopped
